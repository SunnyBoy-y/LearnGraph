# M3–M5 实施清单

> 基线：HEAD=4d6ee1f；本文件与 `doc/memory-producers.md` 随代码维护。

## M3：时间语义 + 计划状态机

- [x] 新增 `backend/app/services/temporal_normalizer.py`，`normalizer_version=temporal-v2`
- [x] 支持 `year/month/week/date/day_period/hour/minute/interval/recurrence/open_ended`
- [x] `observed_at` 使用消息发生时间；无用户时区时回退系统默认并标记 `timezone_source`
- [x] 计划 canonical key：`plan:<normalized subject>`；RESCHEDULE 保持同一 key，不再降级为 CREATE
- [x] 逾期计划进入 `lapsed_unverified`，退出当前摘要与检索投影
- [x] `plan_change_text` 输出自然语言变化，不暴露 Revision 编号
- [x] 提取链路：模型输出 `time_expression`，程序调用 TemporalNormalizer
- [x] `structured_payload.temporal_semantics` 写入规范化结果
- [x] 测试 `backend/tests/memory/test_temporal_normalizer.py`

## M4：领域事件唯一生产者

- [x] 盘点文档 `doc/memory-producers.md`
- [x] 题目作答 -> `learning.evidence_recorded`，唯一生产者 `ExerciseService`
- [x] Agent Run 结束 -> `agent.run_completed` / `agent.run_failed`，`summary_eligibility=excluded`
- [x] 文件索引/失效事件保持独立生产者，不进普通聊天提取器
- [x] 统一使用 `MemoryEventIngestor.ingest()`，`producer` 枚举补齐 `agent`
- [x] 普通提取器只处理 `role=user` 会话消息
- [x] 测试 `backend/tests/memory/test_producers.py`

## M5：检索增强、真实 Manifest、切默认 events

- [x] `ContextBuildView` 拆分 candidate/retrieved/selected/injected/excluded/truncated
- [x] Manifest 条目绑定 `memory_id + content_hash + scope + reason_codes + token_cost`
- [x] 排除原因覆盖 `CROSS_WORKSPACE/EXPIRED/SUPERSEDED/NO_PERMISSION/LOW_RELEVANCE/BUDGET_EXCEEDED`
- [x] `MemoryAccessLog.injected_tokens` 写入真实 token 成本
- [x] 新增历史 Manifest 查询 API：`GET /memory/context/manifests`
- [x] 前端“使用记录”改为读取真实回执
- [x] v2 检索接入 embedding，关闭/失败时降级不阻塞核心召回
- [x] 排序权重对齐文档 9.3：semantic 0.25 / lexical 0.20 / scope 0.15 / entity 0.15 / importance 0.10 / recency 0.10 / confidence 0.05
- [x] 默认 `memory_read_mode=events`、`memory_context_builder_v2=True`
- [x] events 模式下 suggested prompt 不再走 legacy loader
- [x] 测试 `backend/tests/memory/test_read_mode_cutover.py`

## 未完全自动化的剩余项

- [ ] shadow parity 报告需真实聊天样本；当前 shadow 分支与 `memory_retrieval_traces`
      已具备采集能力
- [ ] Profile/Embedding/Projection 重建的持久化任务调度仍沿用现有 durable queue，
      尚未新增独立幂等任务类型
