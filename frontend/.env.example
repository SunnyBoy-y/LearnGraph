# 留空时前端走同源 /api/v1，开发环境由 Vite 代理转发到后端（目标由
# LEARNGRAPH_BACKEND_ORIGIN 指定，默认 http://127.0.0.1:8000），前端端口回退到
# 5174 等任意端口也不受 CORS 影响。仅当 API 部署在其他源时才填绝对地址。
VITE_API_BASE_URL=
# 登录页是否显示“试用 Demo”入口按钮（构建期变量，修改后需重新构建/重建镜像）。
# ⚠️ 这只是按钮显隐：仅隐藏按钮并不能阻止 demo / learn-graph-local 这组公开凭据
#    通过普通登录进入。后端 demo 身份是否可用由 LEARNGRAPH_ENABLE_DEMO_LOGIN 决定
#    （演示数据另由 LEARNGRAPH_ENABLE_DEMO_SEED 控制；见 backend/.env.example）。
VITE_ENABLE_DEMO_LOGIN=true
# Optional bind address for `npm run dev`; use 0.0.0.0 for LAN access.
# LEARNGRAPH_LISTEN_HOST=0.0.0.0

# 通过内网穿透/端口转发从公网访问时，手动配置公网入口（Vite 会把它加入
# allowedHosts 并作为公网访问时的 origin）。未配置则仅本机/局域网可访问。
# 示例：LEARNGRAPH_PUBLIC_ORIGIN=https://your-tunnel.example.com:23350
# LEARNGRAPH_PUBLIC_ORIGIN=
# 额外的 Vite allowedHosts（逗号分隔，域名不带端口）；通常无需手动填，
# 因为 LEARNGRAPH_PUBLIC_ORIGIN 的 hostname 会被自动加入。
# 示例：LEARNGRAPH_ALLOWED_HOSTS=your-tunnel.example.com
# LEARNGRAPH_ALLOWED_HOSTS=
