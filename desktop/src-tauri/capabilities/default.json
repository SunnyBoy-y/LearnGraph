{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "LearnGraph desktop default capability. The main window loads a remote loopback origin and intentionally gets ZERO Tauri IPC (see doc decision §3.5).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
