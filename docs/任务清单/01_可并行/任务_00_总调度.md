# 总调度窗口

> 多窗口并发时的总控面板。**先读本文件再开工**；每完成一个任务，回来在本文件末尾的「进度日志」追加一行。
> 本文件交给「调度方（通常是你）」，各执行窗口不必写它，只需要写自己的任务文件。

## 一个关键规则：query-key 小工具是共享基石

`frontend/src/lib/query-keys.ts`（和它的清理队友 `auth-query-cache.ts`）是几乎所有可并行/不可并行任务
都会遇到的**小文件**。它们没有脏文件锁，所以：

- 多个窗口**同时** import 同一个工具函数没问题，但**谁想在文件里加内容**就得串行，先后落盘；
- 因此 P0-A（唯一需要写 query-keys.ts 的任务）建议排在可并行波次里，并优先提交，
  之后 everyone 都 `import { workspaceQueryKey }` 现有实现即可，**不用再改这个文件**。

## 默认不可并行的主因（识别冲突源）

这几个文件体积大、职责杂，几乎一定会互相覆盖，必须单窗口：

- `frontend/src/features/chat/chat-pages.tsx`
- `frontend/src/features/graph/graph-pages.tsx`
- `frontend/src/components/layout/workspace-shell.tsx`

只要有两个任务同时写它们，就必然冲突。因此 02_不可并行 里只放有序推进的单窗口任务。

## 推荐的启动波次

| 波次 | 窗口 | 任务 | 文件 | 依赖 | 说明 |
| --- | --- | --- | --- | --- | --- |
| 第一波（并行 4） | A | P1-A 前端测试基础设施 | `frontend/` 测试 | 无 | 可与 P0-A 并行，不碰 query-keys.ts |
| | B | P1-B 后端持久化队列 | `backend/` | 无 | 独立后端域 |
| | C | P0-A query-key 工厂 | `frontend/src/lib/query-keys.ts` | 无 | **最先提交**，做共享基石 |
| | D | P2-C 受检沙箱出站策略 | `backend/` | 无 | 独立后端域，不与 B 抢文件 |
| 第二波（单窗口，等 P0-A） | | P0-B chat 域审计 | chat 域 | P0-A | 依赖查询键约定 |
| 第三波（单窗口） | | P0-C graph 域审计 | graph/goal/learning/dashboard | P0-A | |
| 第四波（单窗口） | | P0-D workspace-shell + 清理对齐 | workspace-shell 等 | P0-A + P0-B/C | 与 chat/graph 竞态敏感 |
| 第五波（单窗口） | | P2-A 组件隔离 renderer | 后端组件 | P1-B | |
| | | P2-B MCP stdio runner | 后端 runner/OAuth | P1-B | |

> 后处理建议：把「依赖 P0-A」的几项（P0-B/C/D）理解为一个**软流水线**，逐项排队，
> 不必等 P0-A 完全做完所有花架子，只要 `query-keys.ts` 的键约定先定稿、提交即可流转。

## 不可并行项的排队理由

（详见各任务文件。要点：它们改到同一批大文件，顺序推进可把合并冲突降到最低。）

## 进度日志

- [x] 建立任务清单与两个文件夹（README、总调度、模板、各任务文件草稿）。
- [x] P0-A query-keys.ts 定稿并提交 —— **之后 everyone import 即可**（2026-08-02：`query-keys.ts` 与 `auth-query-cache.ts` 前缀清理对齐已完成；frontend `npm run build` 通过）。
- [ ] 各 `01_可并行` 任务完成时，回到这里追加一行。

## 关联

- [README.md](../README.md)
- [ROADMAP.md](../../ROADMAP.md)