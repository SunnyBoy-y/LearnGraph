<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 原生版主题：无 ActionBar，状态栏/导航栏透明（配合 Compose enableEdgeToEdge 全屏延伸） -->
    <style name="Theme.LearnGraph" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- 分享投喂接收：无界面透明主题（一闪而过，不打断当前 App） -->
    <style name="Theme.LearnGraph.Transparent" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
